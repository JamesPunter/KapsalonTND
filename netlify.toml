[build]
  # `prebuild` runs `sync:content` (Sheets API, else public CSV URLs, else content/csv — see content-templates/README.md).
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
